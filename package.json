{
  "name": "item-fabricator",
  "version": "0.0.1",
  "private": true,
  "description": "Item Fabricator — Star Citizen crafting simulator, dismantling calculator and resource planner.",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev":         "concurrently -n vite,cf -c blue,yellow \"npm run dev:vite\" \"npm run dev:cf\"",
    "dev:vite":    "npm run dev --workspace client",
    "dev:cf":      "wrangler pages dev --proxy 5173 --port 8788",
    "build":       "npm run build --workspace client",
    "deploy":      "node scripts/fetchGameData.mjs && npm run build && wrangler pages deploy client/dist",
    "import:ptu":  "npm run import:ptu  --prefix exporter",
    "import:live": "npm run import:live --prefix exporter"
  },
  "dependencies": {
    "mongodb": "^6.13.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^3.101.0"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": ["react", "vite", "cloudflare-pages", "mongodb", "atlas"],
  "author": "TheSamon",
  "license": "MIT"
}
