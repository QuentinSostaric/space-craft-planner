# CF Pages is already connected to this repo via the dashboard and auto-deploys
# on every push to main. This workflow is disabled — deployment is handled natively.
# To re-enable manual deployment via GitHub Actions, uncomment the content below
# and add CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID to repo secrets.

# name: Deploy to Cloudflare Pages
# on:
#   workflow_dispatch:   # manual trigger only
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       deployments: write
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'npm'
#       - run: npm ci
#       - run: npm run build
#       - uses: cloudflare/wrangler-action@v4
#         with:
#           apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
#           command: pages deploy client/dist --project-name=space-craft-planner --branch=main
