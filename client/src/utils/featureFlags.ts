// Ship component blueprints are prepared ahead of in-game support.
// Keep all related UI disabled until the game exposes actual component blueprints.
export const ENABLE_SHIP_COMPONENT_BLUEPRINTS = false;
