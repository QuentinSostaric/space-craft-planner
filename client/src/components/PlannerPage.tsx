import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

export function PlannerPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h6">Planner — coming soon</Typography>
    </Box>
  );
}
